import {StyleSheet} from 'react-native';
import {colors} from '../../core-constants';

export const Styles = StyleSheet.create({
  borderCurve: {borderRadius: 8},
  textStyle: {color: colors.black, paddingVertical: 10, textAlign: 'center'},
});
